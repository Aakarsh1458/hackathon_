// Demo-safe stubs.
//
// This file intentionally contains no imports so the web build remains bootable
// without camera / ML Kit dependencies. Replace with the full implementation
// once `camera` and `google_mlkit_*` are added to pubspec.

Object computeInputImageRotation({
  required Object description,
  required Object deviceOrientation,
}) =>
    Object();

Object? inputImageFromCameraImage({
  required Object image,
  required Object rotation,
}) =>
    null;
